(function() {
    
    function getEdad() {
        return 100 + 100 + 300;
    }

    const nombre = 'Luis';
    const apellido = 'De la torre';
    const edad = 30;

    const salida = `${nombre} ${apellido} (${getEdad()})`;

    console.log(salida);

})();



